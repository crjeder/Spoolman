## 1. Update CSS design tokens

- [x] 1.1 In `style/spoolman.css`, set `--fg` to `#3D4555` in the `:root` block
- [x] 1.2 Set `--sidebar-bg` to `#F0F2F5` in the `:root` block
- [x] 1.3 Set `--sidebar-fg` to `#3D4555` in the `:root` block
- [x] 1.4 Set `--accent` to `#4DC8E8` in the `:root` block
- [x] 1.5 Set `--accent-fg` to `#1a2433` in the `:root` block (dark text on cyan)
- [x] 1.6 Set `--border` to `#D0D5DE` in the `:root` block
- [x] 1.7 Set `--muted` to `#7A8499` in the `:root` block
- [x] 1.8 Set `--row-hover` to `#E8ECF2` in the `:root` block
- [x] 1.9 Set `--row-even` to `#F5F7FA` in the `:root` block

## 2. Verify dark mode unchanged

- [x] 2.1 Confirm the `body.dark` block in `style/spoolman.css` is untouched (no token values changed)

## 3. Update changelog

- [x] 3.1 Add entry to `CHANGELOG.md` under a new version noting the light theme palette update
